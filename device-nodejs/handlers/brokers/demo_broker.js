module.exports = {
  upstream: function (topic, message) {
        // pass a message up to the next level

  },
  downStream: function (message) {
        // pass a message from the coordinator to the next level
  }
}
